// Next: Create a components folder for shared UI components like Sidebar and Topbar
import { type RouteConfig, index } from "@react-router/dev/routes";

export default [index("routes/home.tsx")] satisfies RouteConfig;
